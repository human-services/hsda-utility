{
	"title": "service",
	"description": "This is service object from the hsda-orchestration service.",
	"type": "array",
	"patternProperties": {
		"^X-": {
			"type": "object"
		}
	},
	"items": {
		"type": "object",
		"properties": {
			"id": {
				"description": "Each entry must have a unique identifier.",
				"type": "string"
			},
			"user_id": {
				"description": "The id for the user.",
				"type": "string"
			},
			"service": {
				"description": "The service name.",
				"type": "string"
			},
			"path": {
				"description": "The path name.",
				"type": "string"
			},
			"verb": {
				"description": "The verb.",
				"type": "string"
			}
		},
		"required": ["id",
		"user_id",
		"path",
		"verb"]
	}
}